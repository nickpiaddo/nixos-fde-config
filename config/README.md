<!-- DOCTOC SKIP -->
# Configuration files

- `no-root-password-install-iso.nix`: to generate a custom NixOS Minimal ISO
- `demo-vm-configuration.nix`: `/etc/nixos/configuration.nix` for configuring
  the demo VM
